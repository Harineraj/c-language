#include<stdio.h>

int main()
{
    int n,i;
    scanf("%d",&n);
    for(i=1;n>1;i++)
    {
        n/=i;
    }
    i--;
    if(n==1)
    printf("%d",i);
    else
    printf("number not factorial any number");
}
